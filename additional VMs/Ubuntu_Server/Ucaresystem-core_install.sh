#!bash
#add the repository
sudo add-apt-repository ppa:utappia/stable
# Update the package manager
sudo apt-get update
# Install the package
sudo apt-get install ucaresystem-core
# print to screen 
print Ucaresystem-core is now installed you can run it utilizing the "sudo ucaresystem-core" command
